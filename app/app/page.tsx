export default function Home() {
  return (
    <main>
      <h1>Pro Se Commons — backend in progress</h1>
      <p>
        The real UI is being built in Lovable against this Supabase project.
        This root route exists only so the app builds; the working
        prototype lives at the repo root as <code>index.html</code>.
      </p>
    </main>
  );
}
